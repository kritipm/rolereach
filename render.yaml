services:
  - type: web
    name: rolereach-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python dashboard.py
    envVars:
      - key: DATABASE_URL
        sync: false
